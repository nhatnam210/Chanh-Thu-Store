@model ChanhThu_Store.Models.AspNetUser

@if (Model.Avatar != null)
{
    <img class="rounded-circle p-1 user-avatar__comment--type"src="@Model.Avatar" />
}
else
{
    <img class="rounded-circle p-1 user-avatar__comment--type" src="~/Content/images/user-default.png" />
}