@model ChanhThu_Store.Models.AspNetUser
@if (Model.Avatar != null)
{
    <img class="rounded-circle p-1 user-avatar__login" src="@Model.Avatar" />
}
else
{
    <img class="rounded-circle p-1 user-avatar__login" src="~/Content/images/user-default.png" />
}
